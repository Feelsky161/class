# Классные работы.
10.11
ООП - строится на классах и обьектах.
пример:
1.дом это класс
2.дома(экземпляры) объекты
Поля - свойства класса
Методы- функция (действия)
Имя класса с большой буквы
Атрибут-это def summa (a,b,c,d), то что в скобках.
Методы экземпляров - вызываются из объекта.
статические методы общие для данного класса.общие данные для класса.
мутаторы позволяют изменять свойства
аксессеры
конструктор -
set/get дать свойства и вернуть
методы конструкторы - вызываются автоматом при создании объекта.
инициализатор __init__ - задать значение.
деструктор метод - удаление объета.логирование записывается лог.всё автоматом.
def __del__(self):
    print (self __ name: murka)
ООП основные принципа:
инкапсуляция-сокрытие данных. То что мы хотим показать пользователю из фунционала.Это представление объекта в совокупность свойств и методов и сокрытия данных.
__ это заприватить
наследование-принцип ооп позволяющий заимствовать свойства и методы родительского класса.Принцип программирования наследование является одним из ключевых понятий в ООП. Он позволяет создавать иерархии классов, где один класс (подкласс) наследует свойства и методы другого класса (суперкласса)
полиморфизм- представление методов схожих по функционалу,но разные по реализации.
 абстракци- без реализации класса
модификаторы доступа - _,__.
(композиция
агрегация
ассоциация)
